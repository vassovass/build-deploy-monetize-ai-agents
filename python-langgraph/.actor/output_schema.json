{
    "$schema": "https://apify.com/schemas/v1/output.ide.json",
    "actorOutputSchemaVersion": 1,
    "title": "Output schema",
    "properties": {
        "results": {
            "type": "string",
            "title": "Results",
            "template": "{{links.apiDefaultDatasetUrl}}/items"
        },
        "response": {
            "type": "string",
            "title": "Response file",
            "template": "{{links.apiDefaultKeyValueStoreUrl}}/records/response.txt"
        }
    }
}
